<section class="card">
<a href="/post/{{id}}">
 <header class="card-header">
      <div>
        <p class="card-title" >{{title}}</p>
        <p class="card-date">Posted by {{user.username}} on {{format_date date_created}}</p>

      </div>
    </header>
<p class="card-content">{{content}}</p>
  </a>
</section>